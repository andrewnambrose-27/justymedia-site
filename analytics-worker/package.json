{
  "name": "justy-media-analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check src/index.js",
    "dev": "wrangler dev",
    "deploy:dry": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply justy-media-analytics --local",
    "db:migrate:remote": "wrangler d1 migrations apply justy-media-analytics --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
